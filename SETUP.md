# 🚀 Руководство по настройке для новых разработчиков

> **Пошаговая инструкция для быстрого старта работы с ASTRAL AI API**

---

## 📋 Содержание

1. [Требования](#требования)
2. [Быстрый старт](#быстрый-старт)
3. [Настройка окружения](#настройка-окружения)
4. [Настройка базы данных](#настройка-базы-данных)
5. [Проверка работоспособности](#проверка-работоспособности)
6. [Полезные команды](#полезные-команды)
7. [Структура проекта](#структура-проекта)
8. [Документация](#документация)

---

## Требования

### Обязательные

- **Python 3.11+** (проверить: `python --version`)
- **Git** (для клонирования репозитория)
- **pip** (менеджер пакетов Python)

### Внешние сервисы

- **DeepSeek API** аккаунт и ключ
  - Регистрация: https://platform.deepseek.com/
  - Получить API ключ в настройках аккаунта

- **Supabase** проект с PostgreSQL + pgvector
  - Регистрация: https://supabase.com/
  - Создать новый проект
  - Включить расширение `pgvector` в настройках БД

---

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd astral-ai-api
```

### 2. Установка зависимостей

```bash
# Создать виртуальное окружение (рекомендуется)
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

```bash
# Скопировать пример файла
cp .env.example .env

# Отредактировать .env файл и заполнить реальными значениями
# Windows:
notepad .env
# Linux/Mac:
nano .env
```

**Минимально необходимые переменные:**
```env
DEEPSEEK_API_KEY=sk-your-key-here
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key
```

### 4. Запуск сервера

```bash
# Development режим
python api/index.py

# Или использовать готовый скрипт (Windows)
start_api_dev.bat
```

Сервер запустится на `http://localhost:5000`

---

## Настройка окружения

### Структура .env файла

```env
# DeepSeek API Key (обязательно)
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# Supabase Configuration (обязательно)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-service-role-key

# Опционально: режим работы
IS_PRODUCTION=false  # true для production, false для development
```

### Получение API ключей

#### DeepSeek API Key

1. Зарегистрируйтесь на https://platform.deepseek.com/
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте ключ (начинается с `sk-`)

#### Supabase Credentials

1. Зарегистрируйтесь на https://supabase.com/
2. Создайте новый проект
3. В настройках проекта найдите:
   - **Project URL** → это `SUPABASE_URL`
   - **Service Role Key** → это `SUPABASE_KEY` (⚠️ секретный ключ!)

---

## Настройка базы данных

### 1. Создание таблиц

Все необходимые таблицы должны быть созданы в Supabase. Проверьте наличие:

- `mods` — основная таблица модов
- `modpacks` — референсные модпаки
- `users` — пользователи и подписки
- `modpack_builds` — сохранённые сборки
- `ai_sort_sessions` — сессии сортировки
- `crash_doctor_sessions` — сессии Crash Doctor

### 2. Включение pgvector

В Supabase SQL Editor выполните:

```sql
-- Проверить наличие расширения
SELECT * FROM pg_extension WHERE extname = 'vector';

-- Если нет, установить:
CREATE EXTENSION IF NOT EXISTS vector;
```

### 3. Миграции

При необходимости выполнить миграции:

```bash
# Миграция для rate limiting (если таблица users уже существует)
# Выполнить SQL из файла migrations/001_add_rate_limiting.sql
# в Supabase SQL Editor
```

---

## Проверка работоспособности

### 1. Проверка здоровья сервера

```bash
curl http://localhost:5000/health
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "service": "ASTRAL AI API",
  "version": "1.0.0"
}
```

### 2. Проверка конфигурации

```bash
# Проверить что переменные окружения загружены
python -c "from api.config import DEEPSEEK_API_KEY, SUPABASE_URL; print('Config OK' if DEEPSEEK_API_KEY and SUPABASE_URL else 'Config ERROR')"
```

### 3. Тестовый запрос (требует JWT токен)

```bash
curl -X POST http://localhost:5000/api/ai/build-board \
  -H "Authorization: Bearer <your-jwt-token>" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Add sodium and lithium",
    "mc_version": "1.21.1",
    "mod_loader": "fabric",
    "max_mods": 10
  }'
```

---

## Полезные команды

### Разработка

```bash
# Запуск development сервера
python api/index.py

# Запуск с автоматической перезагрузкой (если установлен watchdog)
python -m flask run --reload

# Запуск production сервера (Waitress)
waitress-serve --host 0.0.0.0 --port 5000 --threads 4 api.index:app
```

### Отладка

```bash
# Проверить capabilities модов в БД
python scripts/check_caps.py

# Очистить incompatibilities данные
python scripts/clear_incompatibilities.py
```

### Тестирование

```bash
# Проверка импортов
python -c "import api.index; print('Imports OK')"

# Проверка конфигурации
python -c "from api.config import *; print('Config loaded')"
```

---

## Структура проекта

```
astral-ai-api/
├── api/                          # Основной код API
│   ├── index.py                 # Flask сервер (точка входа)
│   ├── config.py                # Конфигурация
│   ├── auth.py                  # JWT аутентификация
│   ├── rate_limiter.py          # Rate limiting
│   │
│   ├── ai_build_v3.py           # V3 сборка (основной)
│   ├── query_planner.py         # Планировщик запросов
│   ├── hybrid_search.py         # Гибридный поиск
│   ├── final_selector.py       # Финальный селектор
│   │
│   ├── architecture_*.py         # Архитектурные компоненты
│   ├── dependency_resolver.py   # Резолвер зависимостей
│   ├── fabric_compat.py         # Fabric совместимость
│   │
│   └── crash_doctor/            # Crash Doctor система
│       ├── analyze.py
│       ├── crash_analyzer.py
│       ├── fix_planner.py
│       └── ...
│
├── scripts/                      # Утилиты и скрипты
│   ├── check_caps.py
│   └── clear_incompatibilities.py
│
├── migrations/                   # SQL миграции
│   └── 001_add_rate_limiting.sql
│
├── docs/                         # Документация
│   ├── CRASH_DOCTOR_PLAN.md
│   └── SUBSCRIPTIONS_LIMITS_PLAN.md
│
├── requirements.txt              # Python зависимости
├── .env.example                  # Пример переменных окружения
├── README.md                     # Основная документация
├── SETUP.md                      # Это руководство
├── РЕЗЮМЕ_ФИНАЛ.md              # Полная архитектура (русский)
└── SEMANTIC_ARCHITECTURE.md     # Техническая архитектура
```

---

## Документация

### Основные документы

1. **README.md** — обзор проекта и быстрый старт
2. **РЕЗЮМЕ_ФИНАЛ.md** — полное описание архитектуры на русском
3. **SEMANTIC_ARCHITECTURE.md** — техническая архитектура (английский)
4. **RATE_LIMITS.md** — документация по rate limiting
5. **api/SECURITY.md** — документация по безопасности
6. **api/PRODUCTION_SERVER.md** — настройка production сервера

### API Endpoints

Основные endpoints описаны в `README.md`:

- `POST /api/ai/build-board` — сборка модпака
- `POST /api/ai/auto-sort` — автоматическая сортировка
- `POST /api/ai/crash-doctor/analyze` — анализ крашей (SSE)
- `GET /api/ai/usage` — информация о лимитах пользователя
- `POST /api/feedback` — отправка фидбека

### Архитектура

Подробное описание архитектуры в:
- **РЕЗЮМЕ_ФИНАЛ.md** — полная картина системы
- **SEMANTIC_ARCHITECTURE.md** — детали реализации

---

## Частые проблемы

### Проблема: "ModuleNotFoundError"

**Решение:**
```bash
# Убедитесь что виртуальное окружение активировано
# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: "DEEPSEEK_API_KEY not found"

**Решение:**
- Проверьте что файл `.env` существует в корне проекта
- Проверьте что переменные правильно заполнены
- Убедитесь что нет пробелов вокруг `=`

### Проблема: "Connection refused" при запросе к Supabase

**Решение:**
- Проверьте `SUPABASE_URL` и `SUPABASE_KEY` в `.env`
- Убедитесь что используете Service Role Key (не anon key)
- Проверьте интернет соединение

### Проблема: "pgvector extension not found"

**Решение:**
- В Supabase SQL Editor выполните: `CREATE EXTENSION vector;`
- Проверьте что расширение включено в настройках проекта

---

## Следующие шаги

После успешной настройки:

1. ✅ Прочитайте **РЕЗЮМЕ_ФИНАЛ.md** для понимания архитектуры
2. ✅ Изучите код в `api/` директории
3. ✅ Попробуйте сделать тестовый запрос к API
4. ✅ Изучите документацию по безопасности (`api/SECURITY.md`)
5. ✅ Ознакомьтесь с rate limiting (`RATE_LIMITS.md`)

---

## Поддержка

Если возникли проблемы:

1. Проверьте логи сервера (выводятся в консоль)
2. Проверьте документацию в `docs/` и `api/`
3. Убедитесь что все переменные окружения настроены правильно
4. Проверьте что база данных настроена корректно

---

**Удачи в разработке! 🚀**

