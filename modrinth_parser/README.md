# Modrinth Parser

Полноценный парсер модов с Modrinth в базу данных Supabase.

## Возможности

✅ **Автоматическая проверка существующих модов** - не дублирует записи  
✅ **Пакетная обработка** - сохраняет по 100 модов за раз  
✅ **Парсинг зависимостей** - получает зависимости через API версий Modrinth  
✅ **AI генерация** - создаёт краткие описания и классифицирует теги через DeepSeek  
✅ **Embeddings** - генерирует векторные представления для поиска  
✅ **Обработка ошибок** - retry механизм, таймауты, graceful degradation

## Установка зависимостей

```bash
pip install requests anthropic sentence-transformers
```

## Запуск

```bash
python modrinth_parser.py
```

## Как работает

1. **Проверка БД** - загружает список ID существующих модов
2. **Загрузка с Modrinth** - получает порции по 100 модов
3. **Фильтрация** - пропускает уже существующие моды
4. **Обработка каждого мода:**
   - Получает зависимости через API версий
   - Генерирует AI summary (DeepSeek)
   - Классифицирует теги (DeepSeek)
   - Генерирует embedding вектор
5. **Сохранение** - пакетная вставка в Supabase
6. **Повтор** - переходит к следующей порции

## Формат зависимостей

Зависимости сохраняются в формате JSON-строки:

```json
{
  "project_id_1": {
    "type": "required",
    "versions": ["1.21.1", "1.20.1"]
  },
  "project_id_2": {
    "type": "optional",
    "versions": ["1.21.1"]
  }
}
```

## Таймауты и защита

- Задержка 0.5 сек между запросами к Modrinth
- Задержка 1 сек между пакетами
- До 3 попыток при ошибках запросов
- Таймаут 10 сек для Modrinth API
- Таймаут 30 сек для Supabase batch insert

## Прерывание

Нажми `Ctrl+C` чтобы остановить парсинг. Прогресс сохранится, при следующем запуске продолжится с того места где остановился (благодаря проверке существующих модов).

## Логи

Парсер выводит детальную информацию:
- ✅ Успешные операции
- ⚠️ Предупреждения
- ❌ Ошибки
- 📊 Статистика
